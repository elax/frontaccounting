default:
  extensions:
    Behat\MinkExtension\Extension:
      base_url: 'http://localhost:80'
      default_session: zombie
      javascript_session: zombie
      zombie: ~
      show_cmd:  'cat %s > /vagrant/fa/zombie.html'

